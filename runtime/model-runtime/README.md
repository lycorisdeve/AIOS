# Model Runtime

The Model Runtime chooses where and how model inference runs.

Responsibilities:

- select local or cloud models
- route privacy-sensitive tasks locally
- manage context window budgets
- manage token budgets
- support fallback models
- support cross-model verification

