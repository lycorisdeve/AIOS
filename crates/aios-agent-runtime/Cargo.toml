[package]
name = "aios-agent-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Agent process runtime for AIOS"
license.workspace = true

[dependencies]
aios-types = { path = "../aios-types" }

[lib]
path = "src/lib.rs"

