# Example: Quote Generation

Goal:

```text
Generate a quote for a customer.
```

Flow:

```text
Goal
-> identify customer
-> query price history
-> query inventory
-> query cost
-> check policy
-> generate quote artifact
-> require approval if discount is high
```

