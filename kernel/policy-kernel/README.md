# Policy Kernel

The Policy Kernel controls what agents are allowed to do.

Responsibilities:

- evaluate permission scopes
- classify risk
- require approval when needed
- block unsafe actions
- enforce data boundary rules
- provide explainable policy decisions

Every tool call must pass through policy evaluation.

