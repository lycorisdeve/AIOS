# Tests

Test groups:

- `contract`: schema and protocol compatibility
- `policy`: policy decision behavior
- `runtime`: agent and workflow runtime behavior
- `shell`: UI shell behavior

Current local checks:

```text
npm run check
cargo test --workspace
```

